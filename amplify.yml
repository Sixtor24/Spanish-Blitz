version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pnpm
        - pnpm install
    build:
      commands:
        - pnpm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
customHeaders:
  - pattern: "**/*"
    headers:
      - key: "Cache-Control"
        value: "public, max-age=31536000, immutable"
  - pattern: "/index.html"
    headers:
      - key: "Cache-Control"
        value: "no-cache, no-store, must-revalidate"
redirects:
  - source: '/assets/<*>'
    target: '/assets/<*>'
    status: 200
  - source: '/api/<*>'
    target: 'https://spanish-blitz-back-production.up.railway.app/api/<*>'
    status: 200
  - source: '/<*>'
    target: '/index.html'
    status: 200
env:
  variables:
    VITE_API_URL: https://thespanishblitz.com
    VITE_WS_URL: wss://spanish-blitz-back-production.up.railway.app